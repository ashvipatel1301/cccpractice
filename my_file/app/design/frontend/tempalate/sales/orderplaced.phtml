<?php 
$orderData = $this->getOrderDetails();
// print_r($orderData);die;
?>
    <style>
        .order-confirmation-container {
            text-align: center;
        }
        .order-confirmation-icon {
            fill: #4CAF50;
            width: 100px;
            height: 100px;
            filter: drop-shadow(0px 0px 10px rgba(0, 0, 0, 0.3));
        }
        .order-confirmation-message {
            margin-top: 20px;
            font-size: 24px;
            color: #4CAF50;
        }
        .order-confirmation-details {
            margin-top: 30px;
            padding: 15px;
            background-color: #ffffff;
            border: 2px solid #4CAF50; /* Added border */
            border-radius: 5px;
            text-align: left;
            max-width: 300px; /* Limiting the width to make it smaller */
            margin: 0 auto; /* Centering the card */
        }
        .order-confirmation-details h2 {
            font-size: 18px; /* Adjusting the font size */
            margin-bottom: 10px; /* Adding some space below the heading */
        }
        .order-confirmation-details p {
            font-size: 14px; /* Adjusting the font size */
            margin-bottom: 8px; /* Adding some space below each paragraph */
        }
        .order-confirmation-button {
            background-color: #4CAF50;
            border: none;
            color: white;
            padding: 15px 32px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
            margin-top: 20px; /* Adjusted the margin-top */
            cursor: pointer;
            border-radius: 5px;
            transition: background-color 0.3s;
        }
        .order-confirmation-button:hover {
            background-color: #45a049;
        }
    </style>

    <div class="order-confirmation-container">
        <svg class="order-confirmation-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
            <circle cx="12" cy="12" r="10" fill="#ffffff" stroke="#4CAF50" stroke-width="2"/>
            <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41L9 16.17z" fill="#4CAF50"/>
        </svg>
        <div class="order-confirmation-message">Your order has been successfully placed!</div>
        
        <!-- Order Details Section -->
        <div class="order-confirmation-details">
            <h2>Order Details:</h2>
            <p>Total Price: <?php echo $orderData->getGrandTotal(); ?></p>
            <p>Order Number:<?php echo $orderData->getOrderNumber(); ?></p>
        </div>

        <a href="<?php echo $this->getUrl('page/index/index')?>" class="order-confirmation-button">Back to Home</a>
    </div>

